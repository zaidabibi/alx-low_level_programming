#include <stdio.h>
/**
 *  main - writes the character n to stdout
 *
 *
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int main(void)
{
printf("%s\n", __FILE__);
return (0);
}
